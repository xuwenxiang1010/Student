<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bootdo.system.dao.CourseDao">

	<select id="get" resultType="com.bootdo.system.domain.CourseDO">
		select
		* 
		from sys_course where course_id = #{value}
	</select>
	
	<select id="list" resultType="com.bootdo.system.domain.CourseDO">
		select
		* 
		from sys_course
		<where>
			<if test="courseId != null and courseId != ''"> and course_id = #{courseId} </if>
			<if test="courseName != null and courseName != ''"> and course_name = #{courseName} </if>
			<if test="courseType != null and courseType != ''"> and course_type = #{courseType} </if>
			<if test="courseSign != null and courseSign != ''"> and course_sign = #{courseSign} </if>
		</where>
		<choose>
			<when test="sort != null and sort.trim() != ''">
				order by ${sort} ${order}
			</when>
			<otherwise>
				order by course_id desc
			</otherwise>
		</choose>
		<if test="offset != null and limit != null">
			limit #{offset}, #{limit}
		</if>
	</select>
	
	<select id="count" resultType="int">
		select count(*) from sys_course
		<where>
			<if test="courseId != null and courseId != ''"> and course_id = #{courseId} </if>
			<if test="courseName != null and courseName != ''"> and course_name = #{courseName} </if>
			<if test="courseType != null and courseType != ''"> and course_type = #{courseType} </if>
			<if test="courseSign != null and courseSign != ''"> and course_sign = #{courseSign} </if>
		</where>
	</select>
	
	<insert id="save" parameterType="com.bootdo.system.domain.CourseDO"
		useGeneratedKeys="true" keyProperty="courseId">
		insert into sys_course
		(
		`course_id`,
		`course_name`,
		`course_type`,
		`course_sign`
		)
		values
		(
		#{courseId},
		#{courseName},
		#{courseType},
		#{courseSign}
		)
	</insert>
	
	<update id="update" parameterType="com.bootdo.system.domain.CourseDO">
		update sys_course
		<set>
			<if test="courseName != null">`course_name` = #{courseName}, </if>
			<if test="courseType != null">`course_type` = #{courseType}, </if>
			<if test="courseSign != null">`course_sign` = #{courseSign},</if>
		</set>
		where course_id = #{courseId}
	</update>
	
	<delete id="remove">
		delete from sys_course where course_id = #{value}
	</delete>

	<delete id="batchRemove">
		delete from sys_course where course_id in
		<foreach item="courseId" collection="array" open="(" separator=","
			close=")">
			#{courseId}
		</foreach>
	</delete>
	
</mapper>